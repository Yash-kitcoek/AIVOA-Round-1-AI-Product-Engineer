import { configureStore, createSlice, PayloadAction } from '@reduxjs/toolkit';
export type Draft={complaint_source:string;customer_name:string;product_name:string;product_strength:string;batch_number:string;affected_quantity:string;manufacturing_date:string;expiry_date:string;originating_site:string;impacted_materials:string;complaint_type:string;country:string;received_date:string;description:string;source_text:string};
const empty:Draft={complaint_source:'',customer_name:'',product_name:'',product_strength:'',batch_number:'',affected_quantity:'',manufacturing_date:'',expiry_date:'',originating_site:'Manufacturing',impacted_materials:'',complaint_type:'',country:'',received_date:new Date().toISOString().slice(0,10),description:'',source_text:''};
const intake=createSlice({name:'intake',initialState:{draft:empty,assessment:null as any,loading:false,error:''},reducers:{setDraft:(s,a:PayloadAction<Partial<Draft>>)=>{s.draft={...s.draft,...a.payload}},setAssessment:(s,a)=>{s.assessment=a.payload},setLoading:(s,a)=>{s.loading=a.payload},setError:(s,a)=>{s.error=a.payload},reset:(s)=>{s.draft=empty;s.assessment=null;s.error=''}}});
export const {setDraft,setAssessment,setLoading,setError,reset}=intake.actions; export const store=configureStore({reducer:{intake:intake.reducer}}); export type RootState=ReturnType<typeof store.getState>;
